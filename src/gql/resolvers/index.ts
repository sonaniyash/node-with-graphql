import { default as author } from "./author.resolvers";
import { default as blog } from "./blog.resolvers";
import { default as comment } from "./comment.resolvers";

export default { author, blog, comment };